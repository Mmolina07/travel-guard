name: travelguard
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  dio: ^5.11.1
  flutter_secure_storage: ^11.0.0
  shared_preferences: ^2.5.5
  google_maps_flutter: ^2.18.0
  geolocator: ^14.0.3

  # Auth (TG-97 / TG-102) y persistencia (TG-92)
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  google_sign_in: ^6.3.0
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/